<Window x:Class="Hackaton.Requests_form"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackaton"
        mc:Ignorable="d"
        Title="Requests_form" Height="450" Width="800" Background="#1E2A3A">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2A3849" Padding="15" Margin="0,0,0,20">
            <TextBlock Text="Подать заявку" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="White" 
                   HorizontalAlignment="Center"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="Ресурс:" Margin="0,0,0,15" Foreground="White" FontSize="14"/>
                        <TextBlock Text="Срок доступа:" Margin="0,0,0,15" Foreground="White" FontSize="14"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBox Name="txtResource" 
                             Margin="0,5,0,10"
                             Height="30"
                             Background="#2A3849"
                             Foreground="White"
                             BorderBrush="#1ABC9C"
                             Text="{Binding Resource}"/>
                        <DatePicker Name="datePicker"
                                Height="30"
                                Background="#2A3849"
                                Foreground="White"
                                BorderBrush="#1ABC9C"
                                Text="{Binding DataTime}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Обоснование:" Margin="0,10,0,5" Foreground="White" FontSize="14"/>
                <TextBox Name="txtReason" 
                     Height="80" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Background="#2A3849"
                     Foreground="White"
                     BorderBrush="#1ABC9C"
                     Text="{Binding Reason}"/>
            </StackPanel>
        </ScrollViewer>

        <Separator Grid.Row="2" Background="#1ABC9C" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" 
                 Background="#E74C3C"
                 Foreground="White" 
                 FontWeight="Bold"
                 Height="35"
                 Padding="20,5"
                 Click="Close_Click"/>
            <Button Content="Подать" 
                 Background="#1ABC9C" 
                 Foreground="White" 
                 FontWeight="Bold"
                 Height="35"
                 Padding="20,5"
                 Margin="15,0,0,0"
                 Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
