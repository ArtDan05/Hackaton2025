<Window x:Class="Hackaton.Status"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackaton"
        mc:Ignorable="d"
        Title="Status" Height="450" Width="800">
    <Grid Background="#1E2A3A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2A3849" Padding="20">
            <TextBlock Text="Статус заявок" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
        </Border>

        <DataGrid x:Name="SecretsGrid" 
                  Grid.Row="1" 
                  Margin="20"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="White"
                  BorderBrush="#1ABC9C"
                  BorderThickness="1"
                  MouseDoubleClick="DoubleClickOnGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" 
                                    Binding="{Binding Resource}" 
                                    Width="*"/>
                <DataGridTextColumn Header="Состояние" 
                                    Binding="{Binding Status}" 
                                    Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="#2A3849" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="RequestStatus" 
                        Content="Закрыть" 
                        Width="100"
                        Height="35"
                        Background="#E74C3C"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="Close_CLic"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>