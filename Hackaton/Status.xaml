<Window x:Class="Hackaton.Status"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackaton"
        mc:Ignorable="d"
        Title="Status" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <TextBlock Text="Статус заявки" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="White" 
                   HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right" >
            <Button x:Name="RequestStatus" Content="Закрыть" Margin="10,0,0,0" Background="#FF0000" Click="Close_CLic"/>
        </StackPanel>

        <DataGrid x:Name="SecretsGrid" Grid.Row="2" Margin="10"
               AutoGenerateColumns="False"
               IsReadOnly="True"
               SelectionMode="Single" MouseDoubleClick="DoubleClickOnGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Resource}" Width="150"/>
                <DataGridTextColumn Header="Состояние" Binding="{Binding Status}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="3">
            <TextBlock x:Name="StatusText" Text="Готов"/>
        </StatusBar>
    </Grid>
</Window>
