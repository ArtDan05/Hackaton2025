<Window x:Class="Hackaton.SecretSeeker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hackaton"
        mc:Ignorable="d"
        Title="Просмотр секрета" Height="500" Width="900" Background="#1E2A3A">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2A3849" Padding="15" Margin="0,0,0,20">
            <TextBlock Text="Просмотр секрета" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Foreground="White" 
                      HorizontalAlignment="Center"/>
        </Border>

        <DataGrid Grid.Row="1" 
                  x:Name="SecretsGrid"
                  Background="#2A3849"
                  Foreground="White"
                  BorderBrush="#1ABC9C"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="100"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding LoginEncrypted}" Width="120"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding PasswordEncrypted}" Width="120"/>
                <DataGridTextColumn Header="Хост" Binding="{Binding Host}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt}" Width="130"/>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#2A3849"/>
                    <Setter Property="BorderBrush" Value="#1ABC9C"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1ABC9C"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="35"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Готово" 
                    Background="#1ABC9C" 
                    Foreground="White" 
                    FontWeight="Bold"
                    Height="35"
                    Width="120"
                    Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>